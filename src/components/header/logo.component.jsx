import React from 'react';
import logo from '../../images/lucy-logo.png';

const Logo = () => (
  <div className="logo">
    <img className="logo-image" src={logo} />
  </div>
);

export default Logo;
